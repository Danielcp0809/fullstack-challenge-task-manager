export const API_URL = process.env.NEXT_PUBLIC_API_URL;

// TODO: implement the functions to call the API endpoints for Tasks CRUD operations

// export async function fetchTasks() {
//   const res = await fetch(`${API_URL}/tasks`);
//   return res.json();
// }
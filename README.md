# 🧩 Fullstack Challenge – Tasks Management App

This is a **technical interview challenge** to evaluate your skills with **Next.js**, **NestJS**, and **Prisma** using a SQLite database and regular skills such as Git branching flow, Clean Code, Code usability, etc.  
You will have **1 hour** to complete the requirements listed below.

---

## ⚖️ Challenge Rules

1. ⛔ **AI tools, Copilot, ChatGPT, or any code generators are strictly prohibited.**  
   Any code visibly generated by AI will disqualify the submission.

2. ✅ **Allowed tools:**
   - Your preferred IDE or text editor (VSCode, WebStorm, etc.)
   - Standard IDE **autocomplete** features.
   - Official documentation (Next.js, NestJS, Prisma, TypeScript, etc.)

3. 🕒 **Time limit:** You have **1 hour total** from the moment you start the exercise.

4. 💡 The goal is to demonstrate:
   - Your ability to read and understand an existing codebase.
   - Your capacity to build a clean, functional, and maintainable fullstack feature.

---

## 🧱 Project Structure

```bash
fullstack-challenge-tasks-management/
│
├── backend/              # NestJS + Prisma backend API
│   ├── prisma/
│   │   ├── schema.prisma # Database schema
│   │   └── seed.ts       # Initial seed data
│   ├── src/
│   │   ├── main.ts       # Application entry point
│   │   ├── app.module.ts
│   │   └── tasks/        # Tasks module
│   ├── .env              # Environment variables
│   └── package.json
│
├── frontend/             # Next.js (TypeScript) frontend
│   ├── app/              # Next.js App Router
│   ├── components/
│   ├── package.json
│   └── .env.local
│
├── .gitignore
└── README.md
```

---

## 🧩 Required Features (100 points total)

| # | Feature | Description | Points |
|---|----------|--------------|--------|
| 1 | **List Tasks (GET)** | Display all tasks fetched from the backend API (`/tasks`). Include pagination and filtering | 20 |
| 2 | **Create Task (POST)** | Provide a simple form to create a new task (title + description). | 20 |
| 3 | **Update Task (PUT)** | Allow editing an existing task’s title or completion status. | 15 |
| 4 | **Delete Task (DELETE)** | Allow deleting a task from the list. | 15 |
| 4 | **UI Styling** | Apply basic styling for a clean and readable layout using CSS, Tailwind, or styled components. | 10 |
| 5 | **API Integration** | All frontend actions (list, create, edit, delete) must use the provided NestJS API. | 10 |
| 6 | **Database Connection** | Prisma must be used correctly to manage the SQLite database (local). | 5 |
| 7 | **Code Quality** | Clean, modular, and type-safe code (no `any`, consistent formatting). | 5 |

---

## 🧠 Optional Features (Bonus)

| # | Feature | Description | Bonus |
|---|----------|--------------|--------|
| 1 | Task filter/search | Filter tasks by completion status or text search. | +10 |
| 2 | UI/UX polish | Clean responsive design using CSS or a component library. | +5 |
| 3 | Loading/Error states | Handle API loading and error states gracefully. | +5 |
| 4 | Sorting | Sort tasks by creation date or name. | +5 |

---

### API Endpoints to Implement

Your backend should provide these REST endpoints in `backend/src/routes/todos.ts`:

```
GET    /tasks           - Return all tasks from db. Include filtering and pagination
POST   /tasks           - Add new task to the array
PUT    /tasks/:id       - Find a task and toggle completed status
DELETE /tasks/:id       - Remove a task from db.
```

## 🚀 Getting Started

### 1️⃣ Prerequisites
Make sure you have installed:
- **Node.js** ≥ 18  
- **npm** or **yarn**

---

### 2️⃣ Backend Setup (NestJS + Prisma)

```bash
cd backend
npm install
```

Create an `.env` file in `/backend`:

```env
DATABASE_URL="file:./prisma/dev.db"
```

Then run:

```bash
# Generate database & seed data
npx prisma migrate dev --name init
npx ts-node prisma/seed.ts

# Start the backend
npm run start:dev
```

Backend will run at:
👉 http://localhost:3000

---

### 3️⃣ Frontend Setup (Next.js)

```bash
cd frontend
npm install
```

Create an `.env.local` file:

```env
NEXT_PUBLIC_API_URL=http://localhost:3000
```

Then start the development server:

```bash
npm run dev
```

Frontend will run at:
👉 http://localhost:3030

---

### 4️⃣ Verify Connection

Visit `http://localhost:3030`  
You should see the frontend connected to the backend at `http://localhost:3000`.

---

## ✅ Submission

When finished:
1. Ensure your app runs correctly with both frontend and backend.
2. Commit all your work to a **public Git repository**.
3. Share the **repository URL**.

---

### 🧠 Evaluation Criteria

- Functional correctness
- Code clarity & modularity
- Error handling & types
- UI simplicity
- Time management

---

Good luck! 🍀  
We’re looking for **clarity, reasoning, and solid fundamentals**, not completeness at all costs.
